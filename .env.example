# Copy to .env and fill locally. Never commit actual credentials.
OPENAI_API_KEY=
OPENAI_MODEL=
# Optional OpenAI-compatible Chat Completions endpoint (must support tool calls).
# OPENAI_BASE_URL=https://your-provider.example/v1
